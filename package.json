{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "npm run build:artifact",
    "build:artifact": "npm --prefix artifacts/yashasvi-health run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck:libs && npm --prefix artifacts/yashasvi-health run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
